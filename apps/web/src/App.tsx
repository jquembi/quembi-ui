import { useEffect, useMemo, useRef, useState, type ReactNode } from 'react';
import { Badge, Button } from '@quembi-ui/react';
import { Icon, type IconName } from './components/Icon';
import { Preview } from './components/Preview';
import { catalog, categories, type CatalogItem, type Category, type Framework, type Plan } from './data/catalog';

type PlanFilter = 'all' | Plan;
type CategoryFilter = 'all' | Category;
const salesEmail = import.meta.env.VITE_SALES_EMAIL?.trim() as string | undefined;
const repoUrl = 'https://github.com/jquembi/quembi-ui';

function Brand() { return <a href="#inicio" aria-label="Quembi UI — início" className="inline-flex items-center gap-2.5 text-white"><span className="brand-symbol" aria-hidden="true">Q<span>.</span></span><span className="text-lg font-bold tracking-[-.045em]">quembi<span className="text-[#b7a7ff]">ui</span></span><span className="ml-1 rounded border border-white/10 px-1.5 py-0.5 text-[9px] font-semibold uppercase tracking-wide text-slate-400">Beta</span></a>; }
function Tag({ children, light = false }: { children: ReactNode; light?: boolean }) { return <span className={light ? 'eyebrow border-violet-200 bg-violet-50 text-violet-700' : 'eyebrow border-violet-300/20 bg-violet-400/10 text-[#c7baff]'}>{children}</span>; }

function SiteHeader({ onPremium }: { onPremium: () => void }) {
  const [mobile, setMobile] = useState(false);
  return <header className="sticky top-0 z-40 border-b border-white/[.07] bg-[#0c0d16]/90 backdrop-blur-xl"><div className="container-app flex h-[74px] items-center justify-between gap-5">
    <Brand />
    <nav aria-label="Navegação principal" className="hidden items-center gap-8 text-[13px] font-medium text-slate-400 md:flex"><a href="#catalogo" className="nav-link">Componentes</a><a href="#vantagens" className="nav-link">Porquê Quembi UI</a><a href="#docs" className="nav-link">Documentação</a><a href="#precos" className="nav-link">Planos</a></nav>
    <div className="hidden items-center gap-3 md:flex"><a href={repoUrl} target="_blank" rel="noreferrer" aria-label="Abrir projecto no GitHub" className="flex size-9 items-center justify-center rounded-lg border border-white/10 text-slate-400 transition hover:border-white/30 hover:text-white"><Icon name="github" size={17}/></a><button className="header-cta" onClick={onPremium}>Explorar Premium <Icon name="arrow-up" size={15}/></button></div>
    <button type="button" aria-expanded={mobile} aria-controls="mobile-nav" aria-label={mobile ? 'Fechar menu' : 'Abrir menu'} className="flex size-10 items-center justify-center rounded-lg border border-white/10 text-white md:hidden" onClick={() => setMobile(v => !v)}><Icon name={mobile ? 'close' : 'menu'} /></button>
  </div>{mobile && <nav id="mobile-nav" aria-label="Navegação móvel" className="container-app flex flex-col gap-1 border-t border-white/10 py-4 text-sm text-slate-200 md:hidden">{[['#catalogo','Componentes'],['#vantagens','Porquê Quembi UI'],['#docs','Documentação'],['#precos','Planos']].map(([href,label])=><a key={href} href={href} className="rounded-lg px-3 py-3 hover:bg-white/10" onClick={()=>setMobile(false)}>{label}</a>)}<button className="mt-2 rounded-lg bg-violet-500 p-3 text-left font-semibold text-white" onClick={()=>{setMobile(false); onPremium();}}>Explorar Premium →</button></nav>}</header>;
}

function Hero({ onExplore }: { onExplore: () => void }) {
  return <section id="inicio" className="relative overflow-hidden border-b border-white/[.05]"><div className="hero-glow pointer-events-none absolute inset-0"/><div className="container-app relative grid items-center gap-14 pb-22 pt-23 lg:grid-cols-[.98fr_1.02fr] lg:gap-8 lg:pb-28 lg:pt-28">
    <div className="relative z-10 max-w-[620px]"><div className="mb-7 inline-flex items-center gap-2 rounded-full border border-violet-300/20 bg-violet-400/[.07] py-1.5 pl-2 pr-3 text-[11px] font-semibold text-[#d1c6ff]"><span className="rounded-full bg-violet-400/20 px-2 py-1 text-[#c0afff]">✦ NEW</span> Componentes para quem constrói o futuro <Icon name="arrow" size={14}/></div>
      <h1 className="max-w-[680px] text-[clamp(3.35rem,6.2vw,5.9rem)] font-bold leading-[1.035] tracking-[-.074em] text-white">Interfaces<br/>incríveis. <span className="text-gradient">Menos<br className="hidden xl:block"/> esforço.</span></h1>
      <p className="mt-7 max-w-[510px] text-base leading-[1.9] text-[#a5a5bb] sm:text-[17px]">Componentes e layouts feitos à medida com Tailwind CSS. Uma biblioteca visual para React e Vue, com recursos gratuitos e experiências Premium.</p>
      <div className="mt-9 flex flex-wrap gap-3"><a href="#catalogo" className="button-primary">Explorar componentes <Icon name="arrow" size={18}/></a><button onClick={onExplore} className="button-outline">Ver layouts Premium <Icon name="arrow-up" size={16}/></button></div>
      <div className="mt-12 flex flex-wrap items-center gap-4 text-xs text-slate-400"><div className="flex -space-x-1.5" aria-hidden="true"><span className="tech-dot bg-[#58d7df]">R</span><span className="tech-dot bg-[#58dc9a]">V</span><span className="tech-dot bg-[#70aafa]">T</span></div><span>React · Vue · Tailwind CSS</span><span className="hidden h-4 w-px bg-white/15 sm:block"/><span className="flex items-center gap-1.5"><Icon name="check" size={15} className="text-[#80ebbf]"/> Acessível e personalizável</span></div>
    </div>
    <div className="relative mx-auto w-full max-w-[620px] lg:translate-x-4"><div className="absolute -inset-7 rounded-[36px] border border-white/[.04]"/><div className="absolute -inset-15 -z-10 rounded-full bg-violet-600/12 blur-3xl"/><div className="float-pill absolute -left-4 -top-8 z-10 flex items-center gap-2 rounded-xl border border-white/10 bg-[#211f37] px-3 py-2.5 text-[11px] font-semibold text-white shadow-2xl sm:-left-9"><span className="flex size-6 items-center justify-center rounded-lg bg-[#b7a7ff] text-[#241948]">✦</span> 100% personalizável</div>
      <div className="hero-window overflow-hidden rounded-[20px] border border-white/15 bg-[#191a29] shadow-[0_35px_110px_rgba(0,0,0,.55)]"><div className="flex h-12 items-center justify-between border-b border-white/10 px-4"><div className="flex gap-1.5"><span className="size-2.5 rounded-full bg-[#ff6c78]"/><span className="size-2.5 rounded-full bg-[#ffd47a]"/><span className="size-2.5 rounded-full bg-[#65d6aa]"/></div><span className="rounded-md bg-white/5 px-4 py-1.5 text-[10px] text-slate-500">quembi-ui / component-preview</span><Icon name="grid" size={15} className="text-slate-500"/></div>
        <div className="flex h-[370px] sm:h-[423px]"><div className="hidden w-[150px] shrink-0 border-r border-white/10 px-3 py-5 sm:block"><div className="mb-7 flex items-center gap-2 px-2 text-[11px] font-semibold text-white"><span className="rounded bg-violet-400 px-1.5 py-1 text-violet-950">◈</span> Studio</div>{['Visão geral','Componentes','Projectos','Definições'].map((label,i)=><div key={label} className={'mb-2 flex items-center gap-2 rounded-md px-2 py-2.5 text-[10px] '+(i===1?'bg-violet-500/20 text-violet-200':'text-slate-500')}><span>{['◫','▦','◈','⚙'][i]}</span>{label}</div>)}</div>
          <div className="min-w-0 flex-1 bg-[#f8f8fc]"><div className="flex h-13 items-center justify-between border-b border-slate-200 bg-white px-4"><span className="text-[12px] font-bold text-slate-900">Os meus componentes</span><span className="rounded-md bg-violet-600 px-2 py-1.5 text-[9px] font-semibold text-white">+ Criar</span></div><div className="p-4 sm:p-5"><div className="mb-4 flex justify-between gap-2"><div><p className="text-[10px] text-slate-400">Biblioteca</p><p className="text-lg font-bold tracking-tight text-slate-900">Design sem limites.</p></div><span className="flex h-6 items-center gap-1 rounded-full bg-emerald-100 px-2 text-[9px] font-bold text-emerald-700">● Live</span></div><div className="grid grid-cols-2 gap-2.5"><div className="col-span-2 rounded-xl border border-slate-200 bg-white p-3 shadow-sm"><div className="mb-3 flex items-center justify-between"><span className="text-[10px] font-semibold text-slate-500">Buttons</span><span className="text-[8px] text-violet-600">View code ↗</span></div><div className="flex flex-wrap gap-2"><span className="rounded-md bg-violet-600 px-3 py-2 text-[10px] font-semibold text-white">Get started →</span><span className="rounded-md border border-slate-200 px-3 py-2 text-[10px] font-semibold text-slate-700">Learn more</span></div></div><div className="rounded-xl border border-slate-200 bg-white p-3 shadow-sm"><p className="mb-3 text-[10px] font-semibold text-slate-500">Status badges</p><div className="flex flex-wrap gap-1"><span className="rounded-full bg-emerald-100 px-2 py-1 text-[9px] font-semibold text-emerald-700">● Active</span><span className="rounded-full bg-violet-100 px-2 py-1 text-[9px] font-semibold text-violet-700">New</span></div></div><div className="rounded-xl border border-slate-200 bg-white p-3 shadow-sm"><p className="mb-2 text-[10px] font-semibold text-slate-500">Input field</p><div className="rounded-md border border-violet-400 px-2 py-2 text-[9px] text-slate-400 shadow-[0_0_0_2px_#ede9fe]">your@email.com</div></div><div className="col-span-2 rounded-xl border border-slate-200 bg-white p-3 shadow-sm"><div className="flex items-center gap-2"><span className="flex size-6 items-center justify-center rounded-full bg-emerald-100 text-[10px] text-emerald-700">✓</span><div><p className="text-[10px] font-bold text-slate-900">Changes saved successfully</p><p className="text-[9px] text-slate-400">Everything is up to date.</p></div></div></div></div></div></div>
        </div><div className="flex h-9 items-center justify-between border-t border-white/10 px-4 text-[9px] text-slate-500"><span className="flex items-center gap-1.5"><span className="size-1.5 rounded-full bg-emerald-400"/> All systems operational</span><span>v0.1.0 · BETA</span></div></div>
      <div className="float-pill-delayed absolute -bottom-7 -right-1 flex items-center gap-3 rounded-xl border border-white/10 bg-[#242239] px-4 py-3 shadow-2xl sm:-right-8"><span className="flex size-9 items-center justify-center rounded-xl bg-emerald-400/15 text-emerald-300"><Icon name="code"/></span><span><span className="block text-[11px] font-semibold text-white">Pronto para usar</span><span className="block text-[10px] text-slate-400">React & Vue</span></span><Icon name="check" size={16} className="text-emerald-300"/></div>
    </div>
  </div><div className="container-app relative pb-10"><div className="flex flex-wrap items-center justify-center gap-x-10 gap-y-4 border-t border-white/[.07] pt-8 text-xs font-medium text-slate-500"><span className="mr-2 text-[10px] uppercase tracking-[.18em] text-slate-600">Construído com</span><span className="flex items-center gap-2 text-[#70dcea]">⚛ <span className="text-slate-400">React</span></span><span className="flex items-center gap-2 text-[#60d1a3]">▲ <span className="text-slate-400">Vue.js</span></span><span className="flex items-center gap-2 text-[#69c5f1]">≈ <span className="text-slate-400">Tailwind CSS</span></span><span className="flex items-center gap-2 text-[#9fb4ff]">TS <span className="text-slate-400">TypeScript</span></span></div></div></section>;
}

function CatalogCard({ item, onOpen }: { item: CatalogItem; onOpen: (item: CatalogItem) => void }) {
  return <article className="group catalog-card flex min-w-0 flex-col overflow-hidden rounded-2xl border border-white/[.09] bg-[#151622]"><div className="preview-area relative flex min-h-[222px] flex-1 items-center justify-center overflow-hidden p-5 text-left">
    <span className="absolute left-4 top-4 z-10 rounded-full border border-slate-200 bg-white/95 px-2.5 py-1 text-[10px] font-bold text-slate-600">{item.category}</span>{item.plan === 'premium' && <span className="absolute right-4 top-4 z-10 inline-flex items-center gap-1 rounded-full bg-[#241a41] px-2.5 py-1 text-[10px] font-bold text-[#d0bfff]"><Icon name="spark" size={11}/> Premium</span>}
    <div className="relative flex w-full items-center justify-center transition-transform duration-300 group-hover:scale-[1.035]"><Preview item={item}/></div>
    <button type="button" onClick={() => onOpen(item)} aria-label={`Pré-visualizar ${item.title}`} className="absolute bottom-3 right-3 flex size-8 items-center justify-center rounded-lg border border-slate-200 bg-white/95 text-slate-600 opacity-0 shadow-md transition group-hover:opacity-100 group-focus-within:opacity-100 focus-visible:opacity-100"><Icon name="eye" size={15}/></button>
  </div><div className="flex min-h-[120px] flex-col justify-between p-5"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h3 className="text-[15px] font-bold tracking-tight text-white">{item.title}</h3><p className="mt-1.5 text-[12px] leading-relaxed text-[#8f90a7]">{item.description}</p></div><span className={'shrink-0 rounded-md border px-2 py-1 text-[10px] font-semibold '+(item.plan==='free'?'border-emerald-300/15 bg-emerald-400/10 text-emerald-300':'border-violet-300/15 bg-violet-400/10 text-violet-300')}>{item.plan === 'free' ? 'Free' : 'Pro'}</span></div><button type="button" onClick={() => onOpen(item)} className="mt-4 inline-flex w-fit items-center gap-1.5 text-[11px] font-bold text-[#b9aaff] transition hover:text-white">{item.plan === 'free' ? 'Ver componente' : 'Ver demonstração'} <Icon name="arrow" size={13}/></button></div></article>;
}

function CatalogSection({ onOpen, onPremium, framework, setFramework }: { onOpen: (item: CatalogItem) => void; onPremium: () => void; framework: Framework; setFramework: (value: Framework) => void }) {
  const [query, setQuery] = useState('');
  const [plan, setPlan] = useState<PlanFilter>('all');
  const [category, setCategory] = useState<CategoryFilter>('all');
  const searchRef = useRef<HTMLInputElement>(null);
  useEffect(() => { function onKey(event: KeyboardEvent) { if ((event.key === '/' || (event.key.toLowerCase() === 'k' && (event.metaKey || event.ctrlKey))) && !(event.target instanceof HTMLInputElement) && !(event.target instanceof HTMLTextAreaElement)) { event.preventDefault(); searchRef.current?.focus(); } } window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, []);
  const filtered = useMemo(() => catalog.filter(item => (plan === 'all' || item.plan === plan) && (category === 'all' || item.category === category) && `${item.title} ${item.description} ${item.category}`.toLowerCase().includes(query.trim().toLowerCase())), [plan, category, query]);
  const clear = () => { setQuery(''); setPlan('all'); setCategory('all'); };
  return <section id="catalogo" className="section-spacing bg-[#0e0f19]"><div className="container-app"><div className="mb-10 flex flex-wrap items-end justify-between gap-6"><div><Tag>✦ A BIBLIOTECA</Tag><h2 className="section-title mt-5">Tudo o que precisas.<br/><span className="text-gradient">Num só lugar.</span></h2><p className="mt-4 max-w-[560px] text-sm leading-[1.9] text-[#999ab0]">Explora os componentes prontos a integrar. Escolhe a tecnologia, encontra o visual perfeito e começa a construir.</p></div><button className="button-outline !h-11" onClick={onPremium}>Explorar layouts Premium <Icon name="arrow-up" size={15}/></button></div>
    <div className="mb-6 flex flex-col gap-4 rounded-2xl border border-white/[.08] bg-[#151622] p-4 lg:flex-row lg:items-center lg:justify-between"><div className="relative flex-1"><Icon name="search" size={18} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-500"/><input ref={searchRef} type="search" aria-label="Pesquisar componentes" placeholder="Pesquisar componentes, layouts..." value={query} onChange={e => setQuery(e.target.value)} className="h-11 w-full rounded-xl border border-white/[.09] bg-[#0f101b] py-2 pl-11 pr-15 text-[13px] text-white outline-none transition placeholder:text-slate-600 focus:border-violet-400/60 focus:ring-2 focus:ring-violet-400/10"/><kbd className="pointer-events-none absolute right-3 top-1/2 hidden -translate-y-1/2 rounded border border-white/10 px-2 py-1 text-[10px] text-slate-600 sm:block">⌘ K /</kbd></div><div className="flex flex-wrap items-center gap-2"><div className="flex rounded-xl border border-white/[.09] bg-[#0f101b] p-1" aria-label="Filtrar por licença">{([['all','Todos'],['free','Free'],['premium','Premium']] as const).map(([v,label])=><button key={v} type="button" aria-pressed={plan===v} onClick={()=>setPlan(v)} className={'rounded-lg px-3 py-2 text-[12px] font-semibold transition '+(plan===v?'bg-[#39324f] text-white':'text-slate-500 hover:text-white')}>{label}</button>)}</div><div className="flex rounded-xl border border-white/[.09] bg-[#0f101b] p-1" aria-label="Seleccionar framework">{(['react','vue'] as Framework[]).map(f=><button key={f} type="button" aria-pressed={framework===f} onClick={()=>setFramework(f)} className={'rounded-lg px-3 py-2 text-[12px] font-semibold transition '+(framework===f?'bg-[#39324f] text-white':'text-slate-500 hover:text-white')}>{f === 'react' ? '⚛ React' : '▲ Vue'}</button>)}</div></div></div>
    <div className="mb-6 flex flex-wrap items-center justify-between gap-4"><div className="flex flex-wrap items-center gap-2"><button onClick={()=>setCategory('all')} aria-pressed={category==='all'} className={'category-pill '+(category==='all'?'category-pill-active':'')}>Todos</button>{categories.map(c=><button key={c} aria-pressed={category===c} onClick={()=>setCategory(c)} className={'category-pill '+(category===c?'category-pill-active':'')}>{c}</button>)}</div><span role="status" className="text-[12px] text-slate-500">{filtered.length} {filtered.length === 1 ? 'resultado' : 'resultados'} · {framework === 'react' ? 'React' : 'Vue'}</span></div>
    {filtered.length ? <div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{filtered.map(item=><CatalogCard key={item.id} item={item} onOpen={onOpen}/>)}</div> : <div className="flex min-h-56 flex-col items-center justify-center rounded-2xl border border-dashed border-white/15 text-center"><Icon name="search" size={26} className="text-slate-500"/><h3 className="mt-3 text-base font-semibold text-white">Nenhum resultado encontrado</h3><p className="mt-1 text-sm text-slate-400">Experimenta uma pesquisa ou um filtro diferente.</p><button onClick={clear} className="mt-5 rounded-lg bg-violet-500 px-4 py-2 text-xs font-semibold text-white">Limpar filtros</button></div>}
    <div className="mt-12 flex flex-col items-center justify-between gap-4 rounded-2xl border border-violet-300/15 bg-gradient-to-r from-[#292040] to-[#1b1b2f] p-6 text-center sm:flex-row sm:text-left"><div className="flex items-center gap-4"><span className="flex size-12 shrink-0 items-center justify-center rounded-xl bg-violet-400/15 text-violet-300"><Icon name="spark" size={23}/></span><div><p className="font-bold text-white">Queres ir ainda mais longe?</p><p className="mt-1 text-[12px] text-slate-400">Explora layouts Premium para dashboards, e-commerce e landing pages.</p></div></div><button className="button-primary !h-11 shrink-0" onClick={onPremium}>Ver Premium <Icon name="arrow" size={15}/></button></div>
  </div></section>;
}

function ValueSection() {
  const benefits: Array<{ icon: IconName; title: string; description: string; number: string }> = [
    { icon: 'bolt', number: '01', title: 'Menos código repetitivo', description: 'Parte de uma base sólida e dedica mais tempo às funcionalidades que realmente importam.' },
    { icon: 'grid', number: '02', title: 'Design consistente', description: 'Componentes com padrões visuais, estados e estilos alinhados em toda a aplicação.' },
    { icon: 'code', number: '03', title: 'React e Vue', description: 'Duas implementações com a mesma linguagem visual, para trabalhares com a tua stack.' },
    { icon: 'shield', number: '04', title: 'Código sob o teu controlo', description: 'Os componentes Free são abertos e editáveis; o Premium requer licença própria.' },
  ];
  return <section id="vantagens" className="section-spacing border-y border-white/[.06] bg-[#11121e]"><div className="container-app"><div className="mb-12 grid gap-6 md:grid-cols-2 md:items-end"><div><Tag>✦ FEITO PARA DEVS</Tag><h2 className="section-title mt-5">Não é só bonito.<br/><span className="text-gradient">É pensado para criar.</span></h2></div><p className="max-w-[470px] text-sm leading-[1.9] text-slate-400">Cada detalhe foi desenhado para tornar o processo de desenvolvimento mais rápido, agradável e consistente, sem impor uma aplicação fechada.</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{benefits.map(b=><div key={b.number} className="benefit-card relative rounded-2xl border border-white/[.07] bg-[#191a28] p-6"><span className="absolute right-5 top-5 text-[11px] font-semibold text-slate-600">{b.number}</span><span className="mb-9 flex size-12 items-center justify-center rounded-xl border border-violet-300/15 bg-violet-400/10 text-violet-300"><Icon name={b.icon} size={22}/></span><h3 className="text-[15px] font-bold text-white">{b.title}</h3><p className="mt-3 text-[12px] leading-[1.85] text-slate-400">{b.description}</p></div>)}</div></div></section>;
}

function Docs({ framework, setFramework }: { framework: Framework; setFramework: (value: Framework) => void }) {
  const [copied, setCopied] = useState(false);
  const code = framework === 'react' ? `import { Button, Badge } from '@quembi-ui/react';\n\nexport default function App() {\n  return (\n    <div className="flex items-center gap-3">\n      <Badge tone="success">Pronto</Badge>\n      <Button>Criar projecto</Button>\n    </div>\n  );\n}` : `<script setup lang="ts">\nimport { UiButton, UiBadge } from '@quembi-ui/vue';\n</script>\n\n<template>\n  <div class="flex items-center gap-3">\n    <UiBadge tone="success">Pronto</UiBadge>\n    <UiButton>Criar projecto</UiButton>\n  </div>\n</template>`;
  async function copyCode() { try { await navigator.clipboard.writeText(code); setCopied(true); window.setTimeout(()=>setCopied(false), 1800); } catch { setCopied(false); } }
  return <section id="docs" className="section-spacing bg-[#0c0d16]"><div className="container-app grid items-center gap-12 lg:grid-cols-2"><div><Tag>✦ DOCUMENTAÇÃO</Tag><h2 className="section-title mt-5">Do zero ao primeiro<br/><span className="text-gradient">componente, rápido.</span></h2><p className="mt-5 max-w-[440px] text-sm leading-[1.9] text-slate-400">O projecto inclui implementações independentes e exemplos prontos para adaptar. Começa pelo monorepo e personaliza os componentes de acordo com o teu produto.</p><div className="mt-7 space-y-4">{[['01', 'Instala as dependências', 'Executa npm install na raiz do projecto.'],['02','Inicia o website','Executa npm run dev para abrir a galeria.'],['03','Integra os componentes','Usa os pacotes locais ou copia o código Free.']].map(([n,title,desc])=><div key={n} className="flex items-start gap-4"><span className="flex size-9 shrink-0 items-center justify-center rounded-lg border border-violet-400/20 bg-violet-400/10 text-xs font-semibold text-violet-300">{n}</span><div><p className="text-[13px] font-semibold text-white">{title}</p><p className="mt-1 text-[12px] text-slate-500">{desc}</p></div></div>)}</div><a href={repoUrl} target="_blank" rel="noreferrer" className="mt-8 inline-flex items-center gap-2 text-[13px] font-bold text-[#bcafff] hover:text-white">Consultar README no GitHub <Icon name="arrow-up" size={16}/></a></div>
    <div className="overflow-hidden rounded-2xl border border-white/10 bg-[#151625] shadow-[0_30px_80px_rgba(0,0,0,.25)]"><div className="flex h-14 items-center justify-between border-b border-white/[.08] px-5"><div className="flex items-center gap-2"><span className="mr-2 size-2 rounded-full bg-emerald-400"/><span className="text-[12px] font-semibold text-white">Quick start</span></div><div className="flex gap-1 rounded-lg bg-black/25 p-1">{(['react','vue'] as Framework[]).map(f=><button key={f} onClick={()=>setFramework(f)} aria-pressed={framework===f} className={'rounded-md px-3 py-1.5 text-[11px] font-semibold '+(framework===f?'bg-[#3c3658] text-white':'text-slate-500')}>{f==='react'?'React':'Vue'}</button>)}</div></div><div className="flex items-center justify-between border-b border-white/[.05] px-5 py-3"><span className="text-[11px] text-slate-400">{framework === 'react' ? 'App.tsx' : 'App.vue'}</span><button onClick={copyCode} className="inline-flex items-center gap-1.5 rounded-md px-2 py-1 text-[11px] text-slate-400 hover:bg-white/5 hover:text-white"><Icon name={copied?'check':'copy'} size={14}/>{copied?'Copiado':'Copiar'}</button></div><pre className="code-area min-h-[280px] overflow-x-auto p-5 text-[12px] leading-[1.95] text-[#d9d4f5]"><code>{code}</code></pre><div className="flex items-center justify-between border-t border-white/[.07] bg-[#11121e] px-5 py-4 text-[11px]"><span className="text-slate-500">TypeScript · Tailwind CSS 4</span><span className="flex items-center gap-1.5 text-emerald-400"><Icon name="check" size={13}/> Free & Open Source</span></div></div>
  </div></section>;
}

function Pricing({ onPremium }: { onPremium: () => void }) {
  return <section id="precos" className="section-spacing border-t border-white/[.06] bg-[#10111c]"><div className="container-app"><div className="mb-11 text-center"><Tag>✦ PLANOS</Tag><h2 className="section-title mt-5">Começa grátis.<br/><span className="text-gradient">Evolui quando quiseres.</span></h2><p className="mx-auto mt-4 max-w-[510px] text-sm leading-[1.8] text-slate-400">Escolhe os componentes abertos ou solicita uma licença para os layouts exclusivos. Os preços comerciais ainda não estão configurados.</p></div><div className="mx-auto grid max-w-[900px] gap-5 md:grid-cols-2"><div className="rounded-2xl border border-white/[.1] bg-[#191a28] p-7"><div className="mb-7 flex items-center justify-between"><span className="text-sm font-bold text-white">Community</span><Badge tone="success">Free</Badge></div><p className="text-4xl font-bold tracking-tight text-white">Grátis</p><p className="mt-3 min-h-11 text-sm leading-relaxed text-slate-400">Para quem quer acelerar o desenvolvimento com uma base aberta.</p><a href="#catalogo" className="mt-7 flex h-12 items-center justify-center rounded-xl border border-white/20 text-sm font-semibold text-white transition hover:bg-white/10">Explorar Free <Icon name="arrow" size={16} className="ml-2"/></a><div className="my-7 h-px bg-white/[.09]"/><p className="mb-4 text-[11px] font-bold uppercase tracking-wider text-slate-500">Incluído no plano</p>{['Componentes React e Vue gratuitos','Código-fonte aberto (MIT)','Layouts essenciais','Personalização com Tailwind CSS'].map(f=><p key={f} className="mb-3 flex items-center gap-2.5 text-[12px] text-slate-300"><Icon name="check" size={15} className="text-emerald-400"/>{f}</p>)}</div><div className="relative rounded-2xl border border-violet-400/50 bg-gradient-to-b from-[#28223f] to-[#191a28] p-7 shadow-[0_0_70px_rgba(126,87,255,.09)]"><span className="absolute -top-3 right-6 rounded-full bg-[#b7a7ff] px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-[#251744]">Para projectos avançados</span><div className="mb-7 flex items-center justify-between"><span className="text-sm font-bold text-white">Premium</span><span className="flex items-center gap-1 text-[11px] font-bold text-violet-300"><Icon name="spark" size={15}/> Pro</span></div><p className="text-4xl font-bold tracking-tight text-white">Sob consulta</p><p className="mt-3 min-h-11 text-sm leading-relaxed text-slate-400">Layouts completos, experiências exclusivas e licenciamento comercial.</p><button onClick={onPremium} className="button-primary mt-7 !w-full">Solicitar acesso <Icon name="arrow" size={17}/></button><div className="my-7 h-px bg-white/[.09]"/><p className="mb-4 text-[11px] font-bold uppercase tracking-wider text-slate-500">Previsto para o plano</p>{['Tudo o que está no Free','Layouts Premium com código privado','Licença comercial por contrato','Actualizações conforme a licença'].map(f=><p key={f} className="mb-3 flex items-center gap-2.5 text-[12px] text-slate-300"><Icon name="check" size={15} className="text-violet-300"/>{f}</p>)}</div></div><p className="mt-7 text-center text-xs text-slate-500">O website demonstra os layouts Premium, mas não efectua pagamentos nem concede licenças automaticamente.</p></div></section>;
}

function ItemDialog({ item, onClose, framework, setFramework }: { item: CatalogItem; onClose: () => void; framework: Framework; setFramework: (framework: Framework) => void }) {
  const [tab, setTab] = useState<'preview' | 'code'>('preview');
  const [copyStatus, setCopyStatus] = useState<'idle' | 'done' | 'error'>('idle');
  const closeRef = useRef<HTMLButtonElement>(null);
  useEffect(() => { const previous = document.activeElement instanceof HTMLElement ? document.activeElement : null; closeRef.current?.focus(); const onKey=(event:KeyboardEvent)=>{if(event.key==='Escape') onClose();}; document.addEventListener('keydown',onKey); const oldOverflow=document.body.style.overflow; document.body.style.overflow='hidden'; return ()=>{document.removeEventListener('keydown',onKey);document.body.style.overflow=oldOverflow;previous?.focus();}; }, [onClose]);
  const code = framework === 'react' ? item.reactCode : item.vueCode;
  async function copy() { if (!code) return; try { await navigator.clipboard.writeText(code); setCopyStatus('done'); } catch { setCopyStatus('error'); } }
  const contact = salesEmail && /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(salesEmail) ? `mailto:${salesEmail}?subject=${encodeURIComponent(`Pedido de acesso Premium — ${item.title}`)}` : null;
  return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-[#070710]/85 p-3 backdrop-blur-md sm:p-6" onMouseDown={e=>{if(e.target===e.currentTarget)onClose();}}><section role="dialog" aria-modal="true" aria-labelledby="dialog-title" className="flex max-h-[min(90vh,820px)] w-full max-w-[780px] flex-col overflow-hidden rounded-2xl border border-white/15 bg-[#191a29] shadow-[0_30px_120px_rgba(0,0,0,.6)]"><div className="flex items-start justify-between gap-4 border-b border-white/10 p-5 sm:p-6"><div className="flex items-start gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-violet-400/10 text-violet-300"><Icon name={item.icon as IconName} size={19}/></span><div><div className="flex flex-wrap items-center gap-2"><h2 id="dialog-title" className="text-lg font-bold text-white">{item.title}</h2><span className={'rounded px-2 py-0.5 text-[10px] font-semibold '+(item.plan==='free'?'bg-emerald-400/10 text-emerald-300':'bg-violet-400/15 text-violet-300')}>{item.plan==='free'?'FREE':'PREMIUM'}</span></div><p className="mt-1 text-xs leading-relaxed text-slate-400">{item.description}</p></div></div><button ref={closeRef} onClick={onClose} aria-label="Fechar janela" className="flex size-9 shrink-0 items-center justify-center rounded-lg text-slate-400 hover:bg-white/10 hover:text-white"><Icon name="close" size={19}/></button></div>
    <div className="flex items-center justify-between gap-4 border-b border-white/10 px-5 py-3 sm:px-6"><div className="flex gap-1 rounded-lg bg-[#10111b] p-1"><button onClick={()=>setTab('preview')} aria-pressed={tab==='preview'} className={'inline-flex items-center gap-1 rounded-md px-3 py-2 text-xs font-semibold '+(tab==='preview'?'bg-[#343049] text-white':'text-slate-400')}><Icon name="eye" size={14}/> Preview</button>{item.plan==='free'&&<button onClick={()=>setTab('code')} aria-pressed={tab==='code'} className={'inline-flex items-center gap-1 rounded-md px-3 py-2 text-xs font-semibold '+(tab==='code'?'bg-[#343049] text-white':'text-slate-400')}><Icon name="code" size={14}/> Código</button>}</div><div className="flex gap-1 rounded-lg border border-white/10 p-1">{(['react','vue'] as Framework[]).map(f=><button key={f} onClick={()=>setFramework(f)} aria-pressed={framework===f} className={'rounded px-2.5 py-1.5 text-[11px] font-semibold '+(framework===f?'bg-violet-500/20 text-violet-200':'text-slate-500')}>{f==='react'?'React':'Vue'}</button>)}</div></div>
    <div className="min-h-0 flex-1 overflow-auto">{tab==='preview'||item.plan==='premium'?<div className="preview-area flex min-h-[300px] items-center justify-center p-6 sm:min-h-[380px] sm:p-10"><div className="flex w-full justify-center"><Preview item={item} expanded/></div></div>:<pre className="code-area min-h-[300px] overflow-auto p-6 text-[12px] leading-7 text-[#d5d0ef]"><code>{code}</code></pre>}</div>
    <div className="flex flex-wrap items-center justify-between gap-3 border-t border-white/10 px-5 py-4 sm:px-6"><p className="max-w-[370px] text-[11px] leading-relaxed text-slate-400">{item.plan==='free'?'O código completo está em packages/react e packages/vue. O botão copia um exemplo de utilização.':'Demonstração visual conceptual. O código Premium não está incluído neste repositório público.'}</p>{item.plan==='free'?<button className="button-primary !h-10 text-xs" onClick={copy}><Icon name={copyStatus==='done'?'check':'copy'} size={15}/>{copyStatus==='done'?'Exemplo copiado':copyStatus==='error'?'Cópia indisponível':'Copiar exemplo'}</button>:contact?<a href={contact} className="button-primary !h-10 text-xs">Solicitar licença <Icon name="arrow" size={15}/></a>:<span className="rounded-lg border border-white/10 px-3 py-2 text-[11px] text-slate-400">Contacto comercial por configurar</span>}</div>
  </section></div>;
}
function PremiumDialog({ onClose }: { onClose: () => void }) {
  const firstRef = useRef<HTMLButtonElement>(null);
  useEffect(()=>{firstRef.current?.focus(); const handler=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();}; document.addEventListener('keydown',handler); const previous=document.body.style.overflow;document.body.style.overflow='hidden';return ()=>{document.removeEventListener('keydown',handler);document.body.style.overflow=previous;};},[onClose]);
  const contact = salesEmail && /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(salesEmail) ? `mailto:${salesEmail}?subject=${encodeURIComponent('Pedido de acesso Quembi UI Premium')}` : null;
  return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-[#070710]/85 p-4 backdrop-blur-md" onMouseDown={e=>{if(e.target===e.currentTarget)onClose();}}><section role="dialog" aria-modal="true" aria-labelledby="premium-dialog-title" className="relative w-full max-w-[500px] rounded-2xl border border-violet-400/30 bg-gradient-to-b from-[#29223f] to-[#191a29] p-7 shadow-2xl sm:p-9"><button ref={firstRef} onClick={onClose} aria-label="Fechar janela" className="absolute right-4 top-4 flex size-9 items-center justify-center rounded-lg text-slate-400 hover:bg-white/10 hover:text-white"><Icon name="close" size={18}/></button><span className="flex size-12 items-center justify-center rounded-xl bg-violet-400/15 text-violet-300"><Icon name="spark" size={23}/></span><Tag>PREMIUM ACCESS</Tag><h2 id="premium-dialog-title" className="mt-5 text-3xl font-bold tracking-tight text-white">Mais possibilidades<br/><span className="text-gradient">para o teu próximo projecto.</span></h2><p className="mt-4 text-sm leading-[1.8] text-slate-400">O catálogo mostra demonstrações visuais dos layouts Premium. O código e as condições comerciais dependem de uma licença separada.</p><div className="mt-6 space-y-3">{['Dashboards, e-commerce e landing pages','Licenciamento comercial por contrato','Código Premium entregue apenas a clientes autorizados'].map(t=><p key={t} className="flex items-start gap-2.5 text-[12px] text-slate-200"><Icon name="check" size={16} className="shrink-0 text-emerald-400"/>{t}</p>)}</div>{contact?<a className="button-primary mt-8 !w-full" href={contact}>Solicitar acesso por e-mail <Icon name="arrow" size={17}/></a>:<div className="mt-8 rounded-xl border border-amber-300/15 bg-amber-300/5 p-4 text-[12px] leading-relaxed text-amber-100">O contacto comercial ainda não foi configurado. Define <code>VITE_SALES_EMAIL</code> no ambiente do website para activar pedidos de acesso.</div>}<p className="mt-4 text-center text-[11px] text-slate-500">Nenhum pagamento ou subscrição é processado neste website.</p></section></div>;
}

export default function App() {
  const [framework, setFramework] = useState<Framework>('react');
  const [selected, setSelected] = useState<CatalogItem | null>(null);
  const [premiumOpen, setPremiumOpen] = useState(false);
  const closeItem = () => setSelected(null);
  const closePremium = () => setPremiumOpen(false);
  const openPremium = () => { setSelected(null); setPremiumOpen(true); };
  return <><SiteHeader onPremium={openPremium}/><main><Hero onExplore={openPremium}/><CatalogSection framework={framework} setFramework={setFramework} onOpen={setSelected} onPremium={openPremium}/><ValueSection/><Docs framework={framework} setFramework={setFramework}/><Pricing onPremium={openPremium}/></main><footer className="border-t border-white/[.07] bg-[#0c0d16]"><div className="container-app flex flex-wrap items-center justify-between gap-6 py-9"><div><Brand/><p className="mt-2 text-xs text-slate-500">Componentes de excelência, para ideias sem limites.</p></div><div className="flex flex-wrap items-center gap-5 text-xs text-slate-500"><a href="#catalogo" className="hover:text-white">Componentes</a><a href="#docs" className="hover:text-white">Docs</a><a href="#precos" className="hover:text-white">Licenças</a><a href={repoUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 hover:text-white"><Icon name="github" size={14}/> GitHub</a></div><p className="w-full border-t border-white/[.06] pt-6 text-[11px] text-slate-600">© 2026 Quembi UI. Componentes Free sob MIT. Premium mediante licença comercial independente.</p></div></footer>{selected && <ItemDialog item={selected} onClose={closeItem} framework={framework} setFramework={setFramework}/>} {premiumOpen && <PremiumDialog onClose={closePremium}/>}</>;
}
