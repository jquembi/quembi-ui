{
  "name": "quembi-ui-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @quembi-ui/web",
    "build": "npm run build -w @quembi-ui/web",
    "preview": "npm run preview -w @quembi-ui/web",
    "typecheck": "npm run typecheck -w @quembi-ui/web",
    "check": "npm run typecheck && npm run build",
    "verify": "node scripts/verify.mjs",
    "verify:deploy": "node scripts/verify-deploy.mjs",
    "smoke": "node scripts/smoke-build.mjs",
    "cloudflare:whoami": "npx --yes wrangler@4 whoami",
    "cloudflare:project:create": "npx --yes wrangler@4 pages project create quembi-ui --production-branch=main",
    "deploy:pages": "npm run verify && npm run verify:deploy && npm run check && npm run smoke && npx --yes wrangler@4 pages deploy apps/web/dist --project-name=quembi-ui --branch=main"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
